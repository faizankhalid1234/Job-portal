### Copy this file to `.env` and replace <db_password> and <dbname> with real values
# Example using your provided Atlas URL. Do NOT commit your real .env to source control.
# Replace <db_password> with the password for user `faizan` and <dbname> with your database name.

MONGO_URI=mongodb+srv://faizan:<db_password>@cluster0.gm0lq4k.mongodb.net/<dbname>?retryWrites=true&w=majority

# Optional: change backend port
PORT=8000
